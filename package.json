{
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^16.0.3",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^12.9.0",
    "cypress-parallel": "^0.12.0"
  },
  "scripts": {
    "cy:run": "cypress run -b chrome --headed",
    "cy:open": "cypress open",
    "cy:parallel" : "cypress-parallel -s cy:run -t 4 -d './cypress/e2e/*/*.feature' -m false"
  }
}
